PORT=4000

# Local MongoDB (default)
MONGODB_URI=mongodb://127.0.0.1:27017/unified_portal

# Required for sign-in tokens (use a long random string in production)
JWT_SECRET=change-this-to-a-long-random-secret

# Optional: if you want to use a specific DB name separately from the URI
# MONGODB_DB=unified_portal

