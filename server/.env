PORT=4000

# Local MongoDB (default)
MONGODB_URI=mongodb://127.0.0.1:27017/unified_portal

# Required for sign-in tokens (use a long random string in production)
JWT_SECRET=change-this-to-a-long-random-secret

# Optional: if you want to use a specific DB name separately from the URI
# MONGODB_DB=unified_portal

# Gmail SMTP (real email delivery)
# EMAIL_PASS = Google "App password" from: Account → Security → 2-Step Verification → App passwords
# (16 characters; spaces are OK — the server strips them). Normal Gmail password will not work.
EMAIL_USER=unifiedportal007@gmail.com
EMAIL_PASS=ique ukvy yupp rnaq
# Optional explicit sender address
MAIL_FROM=

